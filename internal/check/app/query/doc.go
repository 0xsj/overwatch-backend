// Package query reads checks and their chains.
//
// Reads are gated on ORG MEMBERSHIP at the composition root. There is nothing
// per-engagement to narrow — a check is the firm's — decisions/0031 and 0032.
package query
