// Package command starts runs and executes them.
//
// **Nothing here authorises.** The caller's reach is resolved at the composition
// root — `write` on the engagement, raised to `admin` when any step is loud
// (decisions/0033 §7) — because this package cannot see `org`.
package command
