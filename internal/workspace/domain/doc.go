// Package domain holds the workspace.
//
// **The reasoning lives one level up, in internal/workspace.**
//
// One type, and the only rule with any weight in it is that archived is
// terminal — the same shape as identity's account, and for the same reason:
// every record that names it must keep meaning what it meant.
package domain
