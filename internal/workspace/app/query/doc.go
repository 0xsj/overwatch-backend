// Package query answers questions about workspace without changing anything.
//
// **It is empty, and that is the current state rather than an omission.**
//
// What belongs here when it arrives: the switcher's list of an org's live
// workspaces, and whatever the breadcrumb needs to name one. Both are views
// rather than domain aggregates — see internal/identity/app/query for the rules.
package query
