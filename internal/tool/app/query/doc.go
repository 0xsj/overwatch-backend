// Package query reads tools and their mapping versions.
//
// Reads are gated on ORG MEMBERSHIP at the composition root, not here. There is
// nothing per-engagement to narrow: a tool is the firm's, so a member of the
// firm sees it — decisions/0031.
package query
