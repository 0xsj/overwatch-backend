// Package query reads the graph: entities, fragments, the claims between them,
// and ASSETS — which is a view rather than a table, because decisions/0009 asked
// for the word and the query to be the same object.
package query
